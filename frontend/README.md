# React Todo Frontend

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm start
   ```

The app will run at http://localhost:3000 and expects the FastAPI backend at http://localhost:8000.
